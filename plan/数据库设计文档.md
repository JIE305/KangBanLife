# 康伴生活(UniHealth) - 数据库设计文档

> 版本: v1.0 | 数据库: SQLite | 最后更新: 2026-05-11 | 审核: 2026-05-13

> **实现状态说明**: 本文档定义了 15 张表的设计。当前 `schema_lite.sql` 实际建表 6 张（users/articles/posts/post_likes/chat_sessions/chat_messages），含 AI 聊天支持。其余 11 张表为第二阶段计划。详见 [当前实现状态](overview/当前实现状态.md)。

## 表实现状态总览

| 分类 | 表名 | 状态 |
|------|------|------|
| 核心 | users | ✅ 已实现 |
| 核心 | articles | ✅ 已实现 |
| 核心 | posts | ✅ 已实现 |
| 交互 | post_likes | ✅ 已实现 |
| 聊天 | chat_sessions | ✅ 已实现（本文档未收录，见精简版） |
| 聊天 | chat_messages | ✅ 已实现（本文档未收录，见精简版） |
| 健康 | user_health_data | ⏳ 计划中 |
| 健康 | user_trend_data | ⏳ 计划中 |
| 内容 | meal_recommendations | ⏳ 计划中 |
| 运动 | sport_courses | ⏳ 计划中 |
| 运动 | medals | ⏳ 计划中 |
| 运动 | user_medals | ⏳ 计划中 |
| 运动 | sport_matches | ⏳ 计划中 |
| 资源 | first_aid_resources | ⏳ 计划中 |
| 资源 | medicines | ⏳ 计划中 |
| 资源 | medical_facilities | ⏳ 计划中 |
| 记录 | mood_records | ⏳ 计划中 |

---

## 目录

1. [概述](#概述)
2. [核心表](#核心表)
3. [健康追踪表](#健康追踪表)
4. [内容管理表](#内容管理表)
5. [运动模块表](#运动模块表)
6. [资源模块表](#资源模块表)
7. [交互与记录表](#交互与记录表)
8. [完整建表SQL](#完整建表sql)
9. [索引设计](#索引设计)
10. [ER图](#er图)

---

## 概述

- **数据库类型**: SQLite (文件型数据库，零配置)
- **字符集**: UTF-8
- **命名规范**: 表名与字段名使用下划线命名(snake_case)
- **主键策略**: INTEGER PRIMARY KEY AUTOINCREMENT (SQLite自增)
- **时间戳**: 使用 ISO 8601 格式文本存储 (SQLite无DATETIME类型)

共设计 **15 张表**:

| 分类 | 表名 | 说明 |
|------|------|------|
| 核心 | users | 用户表 |
| 核心 | articles | 健康文章 |
| 核心 | posts | 匿名树洞帖子 |
| 健康 | user_health_data | 每日健康记录 |
| 健康 | user_trend_data | 身心趋势数据 |
| 内容 | meal_recommendations | 食堂配餐建议 |
| 运动 | sport_courses | 运动课程 |
| 运动 | medals | 勋章定义 |
| 运动 | user_medals | 用户勋章关联 |
| 运动 | sport_matches | 约跑匹配 |
| 资源 | first_aid_resources | 急救知识 |
| 资源 | medicines | 常备药品 |
| 资源 | medical_facilities | 医疗设施 |
| 交互 | post_likes | 帖子点赞记录 |
| 记录 | mood_records | 心情打卡记录 |

---

## 核心表

### 1. users — 用户表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 用户ID |
| student_id | VARCHAR(20) | NOT NULL, UNIQUE | 学号 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 用户名 |
| password | VARCHAR(255) | NOT NULL | 密码(BCrypt加密) |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| phone | VARCHAR(20) | | 手机号 |
| avatar_url | VARCHAR(255) | | 头像路径 |
| gender | TINYINT | DEFAULT 0 | 0=未知, 1=男, 2=女 |
| height | DECIMAL(5,2) | | 身高(cm) |
| weight | DECIMAL(5,2) | | 体重(kg) |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 注册时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

---

### 2. articles — 健康文章表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 文章ID |
| title | VARCHAR(200) | NOT NULL | 标题 |
| excerpt | VARCHAR(500) | | 摘要 |
| content | TEXT | NOT NULL | 正文内容 |
| tag | VARCHAR(50) | DEFAULT '综合' | 标签/分类 |
| tag_class | VARCHAR(100) | | 标签样式类名 |
| img_url | VARCHAR(255) | | 封面图片URL |
| img_alt | VARCHAR(100) | | 图片替代文本 |
| views | INTEGER | DEFAULT 0 | 阅读量 |
| status | TINYINT | DEFAULT 1 | 1=发布, 0=草稿 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

---

### 3. posts — 匿名树洞帖子表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 帖子ID |
| user_id | INTEGER | FK→users.id, NOT NULL | 发布者ID(不公开) |
| author | VARCHAR(50) | NOT NULL | 匿名显示名(如"校友 #12938") |
| content | VARCHAR(1000) | NOT NULL | 帖子内容 |
| likes | INTEGER | DEFAULT 0 | 点赞数 |
| comments | INTEGER | DEFAULT 0 | 评论数(预留) |
| status | TINYINT | DEFAULT 1 | 1=正常, 0=隐藏 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 发布时间 |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**匿名规则**: 发帖时后端自动生成 `校友 #` + 5位随机数字作为 `author`，不暴露真实用户名。

---

## 健康追踪表

### 4. user_health_data — 每日健康记录

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | 记录ID |
| user_id | INTEGER | FK→users.id, NOT NULL | 用户ID |
| record_date | DATE | NOT NULL | 记录日期 |
| wake_early | TINYINT | DEFAULT 0 | 8:00前起床 (0/1) |
| water_done | TINYINT | DEFAULT 0 | 1500ml饮水完成 (0/1) |
| fresh_veggies | TINYINT | DEFAULT 0 | 摄入新鲜蔬果 (0/1) |
| sleep_early | TINYINT | DEFAULT 0 | 23:30前入睡 (0/1) |
| water_ml | INTEGER | DEFAULT 0 | 当日累计饮水量(ml) |
| steps | INTEGER | DEFAULT 0 | 当日步数 |
| focus_minutes | INTEGER | DEFAULT 0 | 当日专注时间(分钟) |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| updated_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

**约束**: `(user_id, record_date)` 联合唯一，同一天同一用户只有一条记录

---

### 5. user_trend_data — 身心趋势数据

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| user_id | INTEGER | FK→users.id, NOT NULL | 用户ID |
| record_date | DATE | NOT NULL | 日期 |
| physical_score | INTEGER | DEFAULT 50 | 体能评分(0-100) |
| energy_score | INTEGER | DEFAULT 50 | 精力评分(0-100) |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

**约束**: `(user_id, record_date)` 联合唯一

---

## 内容管理表

### 6. meal_recommendations — 食堂配餐建议

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| meal_type | VARCHAR(10) | NOT NULL | breakfast/lunch/dinner |
| icon | VARCHAR(50) | | 图标标识 |
| icon_color | VARCHAR(50) | | 图标颜色类 |
| tag | VARCHAR(50) | | 标签文字 |
| tag_class | VARCHAR(100) | | 标签样式类 |
| title | VARCHAR(100) | NOT NULL | 套餐名称 |
| description | VARCHAR(300) | | 套餐描述 |
| calories | INTEGER | | 热量(kcal) |
| protein | INTEGER | | 蛋白质(g) |
| sort_order | INTEGER | DEFAULT 0 | 排序 |
| status | TINYINT | DEFAULT 1 | 1=启用, 0=停用 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

---

## 运动模块表

### 7. sport_courses — 运动课程

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| title | VARCHAR(100) | NOT NULL | 课程名称 |
| description | VARCHAR(300) | | 描述 |
| img_url | VARCHAR(255) | | 封面图URL |
| img_alt | VARCHAR(100) | | 图片替代文本 |
| duration | VARCHAR(10) | | 时长(如"08:15") |
| intensity | VARCHAR(20) | | 强度(低强度/中强度/高强度) |
| intensity_class | VARCHAR(100) | | 强度样式类 |
| participants | VARCHAR(20) | | 参与人数(如"1.2k") |
| category | VARCHAR(20) | DEFAULT 'all' | stretch/strength/all |
| status | TINYINT | DEFAULT 1 | 1=启用, 0=停用 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

---

### 8. medals — 勋章定义

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| name | VARCHAR(50) | NOT NULL | 勋章名称(如"清晨跑者") |
| icon | VARCHAR(50) | | 图标标识(如"solar:sun-bold") |
| description | VARCHAR(200) | | 获得条件说明 |
| sort_order | INTEGER | DEFAULT 0 | |

---

### 9. user_medals — 用户勋章关联

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| user_id | INTEGER | FK→users.id, NOT NULL | |
| medal_id | INTEGER | FK→medals.id, NOT NULL | |
| achieved_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 获得时间 |

**约束**: `(user_id, medal_id)` 联合唯一

---

### 10. sport_matches — 运动匹配信息

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| user_id | INTEGER | FK→users.id, NOT NULL | 发布者ID(不公开) |
| sport_type | VARCHAR(50) | NOT NULL | 运动类型 |
| tag_class | VARCHAR(100) | | 标签样式 |
| time_slot | VARCHAR(50) | | 时间段 |
| location | VARCHAR(100) | | 地点 |
| description | VARCHAR(500) | | 描述 |
| creator_name | VARCHAR(50) | | 匿名显示名 |
| status | TINYINT | DEFAULT 1 | 1=有效, 0=过期 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

---

## 资源模块表

### 11. first_aid_resources — 急救知识

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| title | VARCHAR(100) | NOT NULL | 标题 |
| description | VARCHAR(300) | | 描述 |
| icon | VARCHAR(50) | | 图标 |
| bg_class | VARCHAR(50) | | 背景样式 |
| border_class | VARCHAR(50) | | 边框样式 |
| icon_class | VARCHAR(50) | | 图标样式 |
| icon_hover_class | VARCHAR(50) | | 悬停样式 |
| title_class | VARCHAR(50) | | 标题样式 |
| desc_class | VARCHAR(50) | | 描述样式 |
| sort_order | INTEGER | DEFAULT 0 | |
| status | TINYINT | DEFAULT 1 | |

---

### 12. medicines — 常备药品分类

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| title | VARCHAR(100) | NOT NULL | 药品分类名 |
| description | VARCHAR(500) | | 药品清单说明 |
| icon | VARCHAR(50) | | 图标 |
| icon_bg | VARCHAR(50) | | 图标背景 |
| icon_color | VARCHAR(50) | | 图标颜色 |
| tag | VARCHAR(50) | | 标签(如"建议备选") |
| tag_class | VARCHAR(50) | | 标签样式 |
| sort_order | INTEGER | DEFAULT 0 | |
| status | TINYINT | DEFAULT 1 | |

---

### 13. medical_facilities — 医疗设施

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| name | VARCHAR(100) | NOT NULL | 设施名称 |
| address | VARCHAR(300) | | 地址 |
| lat | DECIMAL(10,6) | | 纬度 |
| lng | DECIMAL(10,6) | | 经度 |
| distance | VARCHAR(20) | | 距离描述(如"1.2km") |
| walk_time | VARCHAR(20) | | 步行时间(如"步行15分钟") |
| rating | VARCHAR(5) | | 评分(如"4.5") |
| type | VARCHAR(30) | | 类型(三甲医院/二甲医院/药店/诊所/急救中心/卫生院/体检中心/门诊/医务室) |
| tag | VARCHAR(100) | | 标签(如"急诊 24h") |
| tag_class | VARCHAR(100) | | 标签样式 |
| tel | VARCHAR(20) | | 电话 |
| status | TINYINT | DEFAULT 1 | |

---

## 交互与记录表

### 14. post_likes — 点赞记录

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| post_id | INTEGER | FK→posts.id, NOT NULL | 帖子ID |
| user_id | INTEGER | FK→users.id, NOT NULL | 用户ID |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

**约束**: `(post_id, user_id)` 联合唯一，防止重复点赞

**点赞逻辑**: 插入记录时 posts.likes+1，删除记录时 posts.likes-1

---

### 15. mood_records — 心情打卡记录

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK, AUTOINCREMENT | |
| user_id | INTEGER | FK→users.id, NOT NULL | 用户ID |
| mood_type | VARCHAR(20) | NOT NULL | happy/neutral/stressed/sad/tired |
| record_date | DATE | NOT NULL | 日期 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

**约束**: `(user_id, record_date)` 联合唯一(upsert: 同一天覆盖更新)

---

## 完整建表SQL

```sql
-- ============================================
-- 康伴生活 UniHealth 数据库建表脚本
-- 适用于 SQLite
-- ============================================

-- 1. 用户表
CREATE TABLE IF NOT EXISTS users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id VARCHAR(20) NOT NULL UNIQUE,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(20),
    avatar_url VARCHAR(255),
    gender TINYINT DEFAULT 0,
    height DECIMAL(5,2),
    weight DECIMAL(5,2),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 2. 健康文章表
CREATE TABLE IF NOT EXISTS articles (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title VARCHAR(200) NOT NULL,
    excerpt VARCHAR(500),
    content TEXT NOT NULL,
    tag VARCHAR(50) DEFAULT '综合',
    tag_class VARCHAR(100),
    img_url VARCHAR(255),
    img_alt VARCHAR(100),
    views INTEGER DEFAULT 0,
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 3. 匿名树洞帖子表
CREATE TABLE IF NOT EXISTS posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    author VARCHAR(50) NOT NULL,
    content VARCHAR(1000) NOT NULL,
    likes INTEGER DEFAULT 0,
    comments INTEGER DEFAULT 0,
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 4. 每日健康记录表
CREATE TABLE IF NOT EXISTS user_health_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    record_date DATE NOT NULL,
    wake_early TINYINT DEFAULT 0,
    water_done TINYINT DEFAULT 0,
    fresh_veggies TINYINT DEFAULT 0,
    sleep_early TINYINT DEFAULT 0,
    water_ml INTEGER DEFAULT 0,
    steps INTEGER DEFAULT 0,
    focus_minutes INTEGER DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE(user_id, record_date)
);

-- 5. 身心趋势数据表
CREATE TABLE IF NOT EXISTS user_trend_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    record_date DATE NOT NULL,
    physical_score INTEGER DEFAULT 50,
    energy_score INTEGER DEFAULT 50,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE(user_id, record_date)
);

-- 6. 食堂配餐建议表
CREATE TABLE IF NOT EXISTS meal_recommendations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    meal_type VARCHAR(10) NOT NULL,
    icon VARCHAR(50),
    icon_color VARCHAR(50),
    tag VARCHAR(50),
    tag_class VARCHAR(100),
    title VARCHAR(100) NOT NULL,
    description VARCHAR(300),
    calories INTEGER,
    protein INTEGER,
    sort_order INTEGER DEFAULT 0,
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 7. 运动课程表
CREATE TABLE IF NOT EXISTS sport_courses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title VARCHAR(100) NOT NULL,
    description VARCHAR(300),
    img_url VARCHAR(255),
    img_alt VARCHAR(100),
    duration VARCHAR(10),
    intensity VARCHAR(20),
    intensity_class VARCHAR(100),
    participants VARCHAR(20),
    category VARCHAR(20) DEFAULT 'all',
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 8. 勋章定义表
CREATE TABLE IF NOT EXISTS medals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(50) NOT NULL,
    icon VARCHAR(50),
    description VARCHAR(200),
    sort_order INTEGER DEFAULT 0
);

-- 9. 用户勋章关联表
CREATE TABLE IF NOT EXISTS user_medals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    medal_id INTEGER NOT NULL,
    achieved_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (medal_id) REFERENCES medals(id),
    UNIQUE(user_id, medal_id)
);

-- 10. 运动匹配表
CREATE TABLE IF NOT EXISTS sport_matches (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    sport_type VARCHAR(50) NOT NULL,
    tag_class VARCHAR(100),
    time_slot VARCHAR(50),
    location VARCHAR(100),
    description VARCHAR(500),
    creator_name VARCHAR(50),
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 11. 急救知识表
CREATE TABLE IF NOT EXISTS first_aid_resources (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title VARCHAR(100) NOT NULL,
    description VARCHAR(300),
    icon VARCHAR(50),
    bg_class VARCHAR(50),
    border_class VARCHAR(50),
    icon_class VARCHAR(50),
    icon_hover_class VARCHAR(50),
    title_class VARCHAR(50),
    desc_class VARCHAR(50),
    sort_order INTEGER DEFAULT 0,
    status TINYINT DEFAULT 1
);

-- 12. 常备药品分类表
CREATE TABLE IF NOT EXISTS medicines (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title VARCHAR(100) NOT NULL,
    description VARCHAR(500),
    icon VARCHAR(50),
    icon_bg VARCHAR(50),
    icon_color VARCHAR(50),
    tag VARCHAR(50),
    tag_class VARCHAR(50),
    sort_order INTEGER DEFAULT 0,
    status TINYINT DEFAULT 1
);

-- 13. 医疗设施表
CREATE TABLE IF NOT EXISTS medical_facilities (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL,
    address VARCHAR(300),
    lat DECIMAL(10,6),
    lng DECIMAL(10,6),
    distance VARCHAR(20),
    walk_time VARCHAR(20),
    rating VARCHAR(5),
    type VARCHAR(30),
    tag VARCHAR(100),
    tag_class VARCHAR(100),
    tel VARCHAR(20),
    status TINYINT DEFAULT 1
);

-- 14. 帖子点赞记录表
CREATE TABLE IF NOT EXISTS post_likes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    post_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (post_id) REFERENCES posts(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE(post_id, user_id)
);

-- 15. 心情打卡记录表
CREATE TABLE IF NOT EXISTS mood_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    mood_type VARCHAR(20) NOT NULL,
    record_date DATE NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE(user_id, record_date)
);
```

---

## 索引设计

```sql
-- ======== users 表索引 ========
CREATE UNIQUE INDEX IF NOT EXISTS idx_users_student_id ON users(student_id);
CREATE UNIQUE INDEX IF NOT EXISTS idx_users_username ON users(username);
CREATE UNIQUE INDEX IF NOT EXISTS idx_users_email ON users(email);

-- ======== posts 表索引 ========
CREATE INDEX IF NOT EXISTS idx_posts_user_id ON posts(user_id);
CREATE INDEX IF NOT EXISTS idx_posts_created_at ON posts(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_posts_status ON posts(status);

-- ======== articles 表索引 ========
CREATE INDEX IF NOT EXISTS idx_articles_tag ON articles(tag);
CREATE INDEX IF NOT EXISTS idx_articles_status ON articles(status);
CREATE INDEX IF NOT EXISTS idx_articles_created_at ON articles(created_at DESC);

-- ======== user_health_data 表索引 ========
CREATE UNIQUE INDEX IF NOT EXISTS idx_health_user_date ON user_health_data(user_id, record_date);

-- ======== user_trend_data 表索引 ========
CREATE UNIQUE INDEX IF NOT EXISTS idx_trend_user_date ON user_trend_data(user_id, record_date);

-- ======== sport_matches 表索引 ========
CREATE INDEX IF NOT EXISTS idx_matches_created_at ON sport_matches(created_at DESC);

-- ======== medical_facilities 表索引 ========
CREATE INDEX IF NOT EXISTS idx_facilities_type ON medical_facilities(type);

-- ======== post_likes 表索引 ========
CREATE UNIQUE INDEX IF NOT EXISTS idx_likes_post_user ON post_likes(post_id, user_id);

-- ======== mood_records 表索引 ========
CREATE UNIQUE INDEX IF NOT EXISTS idx_mood_user_date ON mood_records(user_id, record_date);
```

---

## ER图

```
┌─────────────────┐     ┌──────────────────────┐
│     users       │     │   user_health_data    │
├─────────────────┤     ├──────────────────────┤
│ id (PK)         │──┐  │ id (PK)              │
│ student_id (U)  │  │  │ user_id (FK)         │◄──┐
│ username (U)    │  │  │ record_date          │   │
│ password        │  │  │ wake_early           │   │
│ email           │  │  │ water_done           │   │
│ phone           │  │  │ fresh_veggies        │   │
│ avatar_url      │  │  │ sleep_early          │   │
│ gender          │  │  │ water_ml             │   │
│ height          │  │  │ steps                │   │
│ weight          │  │  │ focus_minutes        │   │
│ created_at      │  │  └──────────────────────┘   │
│ updated_at      │  │                             │
└─────────────────┘  │  ┌──────────────────────┐   │
       │             │  │   user_trend_data     │   │
       │             │  ├──────────────────────┤   │
       │             │  │ id (PK)              │   │
       │             │  │ user_id (FK)         │◄──┤
       │             │  │ record_date          │   │
       │             │  │ physical_score       │   │
       │             │  │ energy_score         │   │
       │             │  └──────────────────────┘   │
       │             │                             │
       │             │  ┌──────────────────────┐   │
       │             │  │   mood_records        │   │
       │             │  ├──────────────────────┤   │
       │             │  │ id (PK)              │   │
       │             └──┤ user_id (FK)         │◄──┤
       │                │ mood_type            │   │
       │                │ record_date          │   │
       │                └──────────────────────┘   │
       │                                           │
       │  ┌──────────────────────┐                 │
       │  │       posts          │                 │
       │  ├──────────────────────┤                 │
       ├──┤ user_id (FK)         │                 │
       │  │ id (PK)              │                 │
       │  │ author               │                 │
       │  │ content              │                 │
       │  │ likes                │                 │
       │  │ comments             │                 │
       │  │ status               │                 │
       │  └──────────────────────┘                 │
       │         │                                 │
       │         │  ┌──────────────────────┐       │
       │         │  │    post_likes         │       │
       │         │  ├──────────────────────┤       │
       │         └──┤ post_id (FK)         │       │
       │            │ user_id (FK)         │◄──────┘
       │            └──────────────────────┘
       │
       │  ┌──────────────────────┐
       │  │   sport_matches       │
       │  ├──────────────────────┤
       └──┤ user_id (FK)         │
          │ id (PK)              │
          │ sport_type           │
          │ time_slot            │
          │ location             │
          │ description          │
          │ creator_name         │
          └──────────────────────┘

  ┌─────────────────────┐     ┌──────────────────────┐
  │      medals         │     │    user_medals        │
  ├─────────────────────┤     ├──────────────────────┤
  │ id (PK)             │─────┤ medal_id (FK)        │
  │ name                │     │ user_id (FK)→users   │
  │ icon                │     │ achieved_at          │
  │ description         │     └──────────────────────┘
  └─────────────────────┘

  以下是独立表（无外键关联）:

  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
  │     articles        │  │ meal_recommendations │  │   sport_courses     │
  ├─────────────────────┤  ├─────────────────────┤  ├─────────────────────┤
  │ id (PK)             │  │ id (PK)             │  │ id (PK)             │
  │ title               │  │ meal_type           │  │ title               │
  │ excerpt             │  │ title               │  │ description         │
  │ content             │  │ description         │  │ img_url             │
  │ tag                 │  │ calories            │  │ duration            │
  │ tag_class           │  │ protein             │  │ intensity           │
  │ img_url             │  │ icon/icon_color     │  │ category            │
  │ views               │  │ tag/tag_class       │  │ participants        │
  │ status              │  │ sort_order          │  │ status              │
  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘

  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐
  │ first_aid_resources │  │     medicines       │  │ medical_facilities   │
  ├─────────────────────┤  ├─────────────────────┤  ├─────────────────────┤
  │ id (PK)             │  │ id (PK)             │  │ id (PK)             │
  │ title               │  │ title               │  │ name                │
  │ description         │  │ description         │  │ address             │
  │ icon                │  │ icon                │  │ lat / lng           │
  │ *_class (多个样式)   │  │ icon_bg/icon_color  │  │ distance            │
  │ sort_order          │  │ tag / tag_class     │  │ walk_time           │
  └─────────────────────┘  └─────────────────────┘  │ rating              │
                                                    │ type                │
                                                    │ tel                 │
                                                    └─────────────────────┘
```

**关系说明**:
- `users 1:N posts` — 一个用户可以发多个帖子
- `users 1:N post_likes` — 一个用户可以点赞多个帖子
- `posts 1:N post_likes` — 一个帖子可以被多个人点赞
- `users 1:N user_health_data` — 一个用户有多天的健康记录
- `users 1:N user_trend_data` — 一个用户有多天的趋势数据
- `users 1:N mood_records` — 一个用户有多天的心情记录
- `users 1:N sport_matches` — 一个用户可以发布多个匹配
- `users N:M medals` (通过 user_medals) — 用户可以获取多个勋章
- `articles / meals / courses / resources / facilities / medicines` — 独立表，无外键
